\chapter{Technical Preliminaries}

First, we define the sorts, some additional notation, and use them to define the global type.
Afterwards we define some sets and functions to create the processes.

% TODO extend syntax to include function calls -> proofs are 'straight-forward'
% TODO type variable syntax for declaration of sorts
% TODO describe Paxos
% TODO Quorum definition (with all the definition it relies on)?